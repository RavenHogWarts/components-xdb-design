# {{PLUGIN_NAME}}

{{DESCRIPTION}}

## 开发

```bash
# 仓库根目录
pnpm install          # 安装依赖（仅需一次）
pnpm build {{DIR_NAME}}   # 生产构建 → {{OUTPUT_FILE}}
pnpm dev {{DIR_NAME}}     # 监听模式

# 或在本项目目录内
pnpm build
pnpm dev
```

构建产物 `{{OUTPUT_FILE}}` 位于项目根目录，将其放入 XDB 的插件目录即可加载。

## 开发规范

本模板基于 `.agents/skills/xdb-plugin-skills` 约定生成：

- 插件元信息与扩展 ID 带命名空间（`{{PLUGIN_ID}}`）
- 图标使用 PascalCase 的 Lucide 名称（如 `List`、`BarChart2`），不要使用 kebab-case
- 所有 CSS class 使用插件专属前缀 `{{CSS_PREFIX}}`（宿主保留前缀 `components--` 不可用）
- 普通设置控件优先使用 `props.setting.*` 声明式 builder（key 稳定），而非手写 DOM
- `onUpdate` 可重复调用，`onDestroy` 释放资源，`install()` 返回 cleanup
- 修改后可运行校验器检查产物形状：

  ```bash
  node .agents/skills/xdb-plugin-skills/scripts/validate-xdb-plugin.mjs {{OUTPUT_FILE}}
  ```
